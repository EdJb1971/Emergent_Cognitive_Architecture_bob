# Optional direct-backend mode. Normal local development uses Vite's
# same-origin proxy and reads API_KEY only in the local Vite server process.
# Never publish a build containing VITE_API_KEY to untrusted clients.
VITE_BACKEND_URL=
VITE_API_KEY=
